import CallStatsCalculator from '@/components/CallStatsCalculator';

export default function CallsPage() {
  return (
    <main style={{ padding: '40px', minHeight: '100vh' }}>
      <CallStatsCalculator />
    </main>
  );
}